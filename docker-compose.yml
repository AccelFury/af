# Generated by AccelFury IP Toolchain
services:
  af-oss:
    build:
      context: .
      dockerfile: Dockerfile
    image: accelfury-af:oss
    working_dir: /work
    environment:
      AF_OFFLINE: "true"
      AF_BUILD_ROOT: /work/.af-build/docker-smoke
      CARGO_HOME: /cargo
      RUSTUP_HOME: /rustup
    volumes:
      - .:/work
      - af-cargo:/cargo
      - af-rustup:/rustup
    command: ["scripts/docker-smoke.sh"]

volumes:
  af-cargo:
  af-rustup:
