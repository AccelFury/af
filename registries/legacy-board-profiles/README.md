# Legacy Board Profiles

These TOML files preserve the original MVP board profile placeholders for
`tang-nano-20k` and `tang-primer-20k`.

They are not canonical board records. Use `registries/board_aliases.json` to
resolve legacy IDs to canonical registry IDs.
